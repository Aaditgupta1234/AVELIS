/**
 * @fileoverview JWT token generation utility.
 *
 * Placeholder for future JWT token generation.
 * Will be implemented when authentication is added.
 *
 * @module utils/generateToken
 *
 * @example
 * // Future usage:
 * // import { generateToken } from '../utils/generateToken.js';
 * // const token = generateToken(user._id);
 */

/**
 * Generate a JWT token for a given user ID.
 *
 * @placeholder This function is not yet implemented.
 * It will use jsonwebtoken once authentication is added.
 *
 * @param {string} _userId - The user's MongoDB ObjectId
 * @returns {string} Placeholder — returns empty string
 */
export const generateToken = (_userId) => {
  // TODO: Implement JWT token generation
  // - Install jsonwebtoken
  // - Import JWT secret and expiry from config
  // - Sign and return the token
  return '';
};
