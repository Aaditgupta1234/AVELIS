/**
 * @fileoverview Collection model.
 *
 * Placeholder for the Mongoose Collection schema and model.
 * Represents a curated collection or shelf of books.
 *
 * @module models/Collection
 *
 * Future schema fields:
 * - name          {String}     — Collection name
 * - description   {String}     — Collection description
 * - books         {ObjectId[]} — References to Book documents
 * - createdBy     {ObjectId}   — Reference to User who created it
 * - isPublic      {Boolean}    — Visibility flag
 * - coverImage    {String}     — Collection cover image URL
 * - createdAt     {Date}       — Auto-generated timestamp
 * - updatedAt     {Date}       — Auto-generated timestamp
 */

// TODO: Define Mongoose schema and export model
// import mongoose from 'mongoose';
// const collectionSchema = new mongoose.Schema({ ... }, { timestamps: true });
// export default mongoose.model('Collection', collectionSchema);
