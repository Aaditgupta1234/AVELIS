/**
 * @fileoverview User service.
 *
 * Contains business logic for user management operations.
 * Called by user controller — does NOT interact with HTTP
 * request/response objects directly.
 *
 * @module services/users/userService
 *
 * Future methods:
 * - getAllUsers(queryOptions) — List users with pagination
 * - getUserById(id)          — Find a single user
 * - updateUser(id, data)     — Update user profile
 * - deleteUser(id)           — Remove a user
 * - updateUserRole(id, role) — Change user role
 */

// TODO: Import User model and implement service methods
// import User from '../../models/User.js';
