/**
 * @fileoverview Auth service.
 *
 * Contains business logic for authentication operations.
 * Called by auth controller — does NOT interact with HTTP
 * request/response objects directly.
 *
 * @module services/auth/authService
 *
 * Future methods:
 * - register(userData)     — Create a new user account
 * - login(credentials)     — Verify credentials and generate token
 * - logout(userId)         — Invalidate user session
 * - refreshToken(token)    — Generate new access token
 * - getCurrentUser(userId) — Retrieve current user details
 */

// TODO: Import User model and implement service methods
// import User from '../../models/User.js';
